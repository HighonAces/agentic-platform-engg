apiVersion: platform.example.io/v1alpha1
kind: BucketClaim
metadata:
  name: team-1-github-bucket
  namespace: team-1
spec:
  parameters:
    location: US
    storageClass: STANDARD
    versioning: false